# Example Outputs

This folder contains selected generated images used as showcase examples in the repository README.

The full `outputs/` folder is ignored by Git because generated images can become large. Only selected images should be copied here manually or through project scripts.
